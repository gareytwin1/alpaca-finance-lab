# Trading bot + dashboard
alpaca-py>=0.40.0      # official Alpaca SDK (replaces the EOL alpaca-trade-api)
pandas>=2.0
numpy>=1.24
flask>=3.0             # dashboard only

# dev / type-checking only: pandas' bundled type hints can't distinguish
# Series from DataFrame in several spots; pandas-stubs fixes that for
# pyright/Pylance without touching runtime behavior.
pandas-stubs
